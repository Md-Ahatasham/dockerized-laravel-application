apiVersion: v1
kind: Service
metadata:
  name: sham-laravel-app-latest
spec:
  selector:
    app: sham-laravel
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer